'use strict';
/**
 * Точка входа для Vercel.
 *
 * Vercel ищет в корне проекта файл app.js, который подключает express,
 * и запускает экспортированное приложение как функцию. Порт не открывается:
 * запросы передаёт сама платформа.
 *
 * Локально и на своём сервере магазин запускается как обычно: npm start
 * (server/index.js). Код один и тот же.
 */
require('express');

module.exports = require('./server/index.js').app;
